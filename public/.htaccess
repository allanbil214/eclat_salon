# ÉCLAT — pretty URLs. Point your web root at this /public folder.
<IfModule mod_rewrite.c>
    RewriteEngine On

    # Serve real files and directories as-is (assets, etc.).
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    # Everything else goes through the front controller.
    RewriteRule ^ index.php [L]
</IfModule>

# Sensible caching for static assets (the ?v= stamp busts the cache on change).
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 month"
</IfModule>

# --- nginx note (for the README) -------------------------------------------
# location / { try_files $uri $uri/ /index.php?$query_string; }
